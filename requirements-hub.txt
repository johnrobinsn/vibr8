# Hub server dependencies (full ML stack + web server)
# Includes everything from pyproject.toml that isn't in requirements-node.txt
# Install on top of requirements-node.txt + requirements-node-desktop.txt

# ML / inference
torch>=2.0
torchaudio>=2.0
transformers>=4.30
bitsandbytes>=0.43
accelerate>=0.30
scipy>=1.10
onnxruntime>=1.15

# HuggingFace model management
huggingface-hub>=0.20

# Server / API
aiohttp>=3.9
python-dotenv>=1.0
json5>=0.9
bcrypt>=4.0
mcp>=1.0
httpx>=0.27

# Computer-use
pillow>=12.2.0
mss>=10.1.0
